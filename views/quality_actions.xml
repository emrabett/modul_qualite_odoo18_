<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Action serveur pour Quality Alert sur stock.picking -->
    <record id="stock_picking_action_quality_alert1" model="ir.actions.server">
        <field name="name">Quality Alert</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="binding_model_id" ref="stock.model_stock_picking"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">
        if record:
            action = record.button_quality_alert1()
        </field>
    </record>

    <!-- Action serveur pour Quality Check sur stock.picking -->
    <record id="stock_picking_action_quality_check1" model="ir.actions.server">
        <field name="name">Quality Check</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="binding_model_id" ref="stock.model_stock_picking"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">
        if record:
            action = record.check_quality1()
        </field>
    </record>

    <!-- Action serveur pour Quality Alert sur mrp.production -->
    <record id="mrp_production_action_quality_alert1" model="ir.actions.server">
        <field name="name">Quality Alert</field>
        <field name="model_id" ref="mrp.model_mrp_production"/>
        <field name="binding_model_id" ref="mrp.model_mrp_production"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">
        if record:
            action = record.button_quality_alert1()
        </field>
    </record>

    <!-- Action serveur pour Quality Check sur mrp.production -->
    <record id="mrp_production_action_quality_check1" model="ir.actions.server">
        <field name="name">Quality Check</field>
        <field name="model_id" ref="mrp.model_mrp_production"/>
        <field name="binding_model_id" ref="mrp.model_mrp_production"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">
        if record:
            action = record.check_quality1()
        </field>
    </record>

    <!-- Action serveur pour Quality Check on Demand sur stock.picking -->
    <record id="stock_picking_action_quality_check_on_demand1" model="ir.actions.server">
        <field name="name">Quality Check on Demand</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="binding_model_id" ref="stock.model_stock_picking"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">
        if record:
            action = record.action_open_on_demand_quality_check1()
        </field>
    </record>

    <!-- Action serveur pour Quality Check on Demand sur mrp.production -->
    <record id="mrp_production_action_quality_check_on_demand1" model="ir.actions.server">
        <field name="name">Quality Check on Demand</field>
        <field name="model_id" ref="mrp.model_mrp_production"/>
        <field name="binding_model_id" ref="mrp.model_mrp_production"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">
        if record:
            action = record.action_open_on_demand_quality_check1()
        </field>
    </record>
</odoo>
